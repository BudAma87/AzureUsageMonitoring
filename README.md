# AzureUsageMonitoring MCP Server

This project reads Azure usage CSVs, generates structured reports, and sends them to Claude Desktop for visualization.

## Run the App

```bash
pip install -r requirements.txt
python main.py
```

Ensure Claude Desktop is running and available to receive reports.